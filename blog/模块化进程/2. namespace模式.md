# namespace 模式

```javascript
var myModule = {
  data: "Naprte",
  foo() {
    console.log(`foo() ${this.data}`);
  },
  bar() {
    console.log(`bar() ${this.data}`);
  },
};
myModule.data = "能直接修改模块内部的数据";
myModule.foo();
```
